# Superwarp Toolkit install defaults (repo-local)
# Set any of these values in env-style syntax (KEY=value).
# Values used by install.sh:
# - 1 / true / yes / on / enabled
#   => auto-install optional components (equivalent to --accept-defaults)

WARP_AI_ACCEPT_DEFAULTS=1
